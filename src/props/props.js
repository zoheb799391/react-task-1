import React from 'react'

const Proper = (props) => {
  return (
    <>
  <p>details</p>
    <h1> {props.name}</h1>
    <h1> {props.age}</h1>
    <h1> {props.email}</h1>
   

    </>
  )
}

export default Proper;